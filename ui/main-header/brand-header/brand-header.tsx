import Logo from '../logo/logo';

import { BrandHeaderContainer } from './brand-header.styles';

const BrandHeader = () => {
  return (
    <BrandHeaderContainer>
      <Logo />
    </BrandHeaderContainer>
  );
};

export default BrandHeader;
